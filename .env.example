# ===================================================
# AMS HACKATHON 2026 - ENVIRONMENT CONFIG TEMPLATE
# ===================================================

# Server Execution Settings
PORT=
NODE_ENV=

# Database Settings (Server-side only)
MONGODB_URI=

# JWT Authentication (Server-side only)
JWT_SECRET=

# Admin Initial Credentials (Server-side only)
ADMIN_USERNAME=
ADMIN_PASSWORD=

# Razorpay Payment Gateway (Server-side only)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# Email SMTP Credentials (Server-side only)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
ORGANIZER_NOTIFICATION_EMAIL=

# Frontend API Endpoint (Client-side)
VITE_API_BASE_URL=
